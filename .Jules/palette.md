## 2026-01-15 - Icon-Only Navigation Links
**Learning:** The navigation bar used icon-only links without accessible names, making them invisible to screen reader users. This is a common pattern in dashboard-like interfaces where visual space is prioritized over semantics.
**Action:** Always add `aria-label` to icon-only links or buttons to ensure they have an accessible name.
