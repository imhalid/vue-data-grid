## 2026-01-31 - Icon-Only Links need Labels
**Learning:** Icon-only links (like settings/profile buttons) are invisible to screen readers without explicit labels.
**Action:** Always add `aria-label` to links or buttons that contain only icons.
