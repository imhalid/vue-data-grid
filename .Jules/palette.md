## 2024-10-25 - Icon-only buttons accessibility
**Learning:** Icon-only interactive elements (like the NavBar buttons) are invisible to screen readers without explicit labels, making navigation impossible for some users.
**Action:** Always verify that buttons or links without text content include an `aria-label` attribute describing their function.
