## 2026-02-01 - Icon-Only Links Missing Context
**Learning:** The application uses icon-only links for primary navigation (Settings, Logout) without accessibility labels, rendering them unusable for screen readers.
**Action:** Audit all icon-only interactive elements and enforce `aria-label` or `title` attributes.
