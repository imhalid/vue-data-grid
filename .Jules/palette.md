## 2024-05-22 - Semantic Buttons for Filters
**Learning:** Using `div` with `onClick` for main interactive elements completely blocks keyboard users.
**Action:** Always refactor legacy `div` "buttons" to `<button>` tags with reset styles and `:focus-visible` indicators.
