## 2024-05-22 - Icon-Only Button Pattern
**Learning:** Found interactive elements (filters) implemented as non-semantic `div`s without keyboard support or ARIA labels.
**Action:** Replace with `<button>` tags, add `aria-label`, and reset CSS styles to maintain design while gaining native accessibility.
