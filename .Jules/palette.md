## 2024-05-21 - Icon Component Accessibility Pattern
**Learning:** The `Icon` component explicitly sets `aria-hidden="true"`, effectively hiding it from screen readers.
**Action:** Always provide an `aria-label` or visible text when wrapping `Icon` in an interactive element (button, link).
